import Stackable from '../../src/stackable-javascript';

describe('Stackable', () => {
  describe('Greet function', () => {
    //beforeEach(() => {
    //  spy(Stackable, 'greet');
    //  Stackable.greet();
    //});
    //
    //it('should have been run once', () => {
    //  expect(Stackable.greet).to.have.been.calledOnce;
    //});
    //
    //it('should have always returned hello', () => {
    //  expect(Stackable.greet).to.have.always.returned('hello');
    //});
  });
});
